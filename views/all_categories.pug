extends layout 
block content

  h1 All Product Categories
  button 
    a(href='/categories/add/') Add Category
  button 
    a(href='/categories/delete/') Delete Category
  ul 
    each category in categories 
      li
        a(href=category.url) #{category.name}